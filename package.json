{
  "name": "sideform",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @sideform/web dev",
    "build": "pnpm -r run build",
    "db:generate": "pnpm --filter @sideform/web db:generate",
    "db:migrate": "pnpm --filter @sideform/web db:migrate",
    "db:push": "pnpm --filter @sideform/web db:push"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=20"
  }
}
