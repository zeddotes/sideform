# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/sideform

# Session (required – 32+ chars)
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# Optional: Redis for sessions / rate limiting
# REDIS_URL=redis://localhost:6379

# AWS (for KMS, S3, SES – set when using invite email / file storage)
# KMS_KEY_ID=
# S3_BUCKET=
# AWS_REGION=us-east-1
# SES_FROM_EMAIL=noreply@example.com
# APP_BASE_URL=http://localhost:3000

# Client-only (VITE_* – only if WS / TURN on different host)
# VITE_WS_URL=ws://localhost:3001
# VITE_TURN_URLS=stun:stun.l.google.com:19302
# VITE_TURN_USERNAME=
# VITE_TURN_CREDENTIAL=
